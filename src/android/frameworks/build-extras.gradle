allprojects {
    repositories {
        maven { url 'https://maven.google.com' }
        mavenCentral();
        jcenter()
    }
}

android {
  defaultConfig {
    compileSdkVersion 26
    targetSdkVersion 26
  }

   packagingOptions {
     exclude 'README'
     exclude 'LICENSE'
   }
}
