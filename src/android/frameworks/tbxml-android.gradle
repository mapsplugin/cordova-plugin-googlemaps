repositories {
  //maven { url 'https://maven.google.com' }
  //mavenCentral()
  //jcenter()
  def libsDirPath = System.getProperty("user.dir")
  flatDir{
    dirs 'src/main/libs',
          'libs',
          "${libsDirPath}/libs",
          "${libsDirPath}/capacitor-cordova-android-plugins/src/main/libs"
  }
}

dependencies {
  implementation(name:'tbxml-android', ext:'aar')
}

android {

  packagingOptions {
    exclude 'README'
    exclude 'LICENSE'
  }
}
